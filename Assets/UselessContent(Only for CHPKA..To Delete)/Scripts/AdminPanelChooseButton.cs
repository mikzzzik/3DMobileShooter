using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class AdminPanelChooseButton : MonoBehaviour
{

    [SerializeField] private List<GameObject> _toHidePanelList;
    [SerializeField] private GameObject _selectedPanel;

    [SerializeField] private int _needAccessLevel = 0;

    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
